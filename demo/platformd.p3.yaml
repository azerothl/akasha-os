# Config plateforme pour le Gate P3 : timeout de confirmation court (3 s)
# pour tester le fail-closed sans attendre 2 minutes.

bus: "127.0.0.1:47001"
audit_dir: var/audit
storage_dir: var/storage
memory_dir: var/memory
modules_dir: var/modules
confirm_timeout_sec: 3
secrets_file: var/secrets/keys.yaml
net_mode: online

embed_model:
  path: tools/models/qwen2.5-0.5b-instruct-q4_k_m.gguf
  n_gpu_layers: 999
  n_threads: 8
