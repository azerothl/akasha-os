[package]
name = "module-ext-rt"
description = "Runtime scripté WASM pour modules agent (handlers.yaml)"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
aos-module-sdk = { path = "../sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
