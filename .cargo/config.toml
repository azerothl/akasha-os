[build]
# Les warnings « Accès refusé » de finalisation incrémentale sous Windows
# (antivirus/verrous) bruitent les builds ; P0/P1 n'ont pas besoin de
# l'incrémental (workspace petit hors llama.cpp, qui est déjà mis en cache).
incremental = false
